version: "3.3"

services:
  backend:
    build: ./backend
    command: npm run dev
    ports:
      - "3001:3001"
    volumes:
      - ./backend:/usr/app
  db:
    image: postgres
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: example
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
